6/8    (1)CPP不能直接声明边长数组要用vector
       (2)声明二维数组时注意溢出问题
       (3)边长二维数组声明方式 vector<vector<int> >   a(n,vector<int> (n));
       (4)变量、数据类型声明后要尽快读取、初始化
       (5)注意变量的动态变化

6/9    (1)递归函数的数组参数要引用参数

6/28   (1)条件判断也是按顺序的
       (2)删除以p为头指针的数组： delete[] p;
       (3)递归调用函数时，注意有时要用return来调用，保证正常终止
